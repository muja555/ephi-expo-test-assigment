import { StyleSheet } from "react-native";
import Colors from "core/style/colors";

const ephiFormContainerStyle = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: Colors.White,
        paddingHorizontal: 16,
    },
    header: {
        height: 40
    },
    backBtn: {
        height: 40,
        width: 30
    },
})
export { ephiFormContainerStyle as ephiFormContainerStyle };

